---
import { Users, FileText, Zap } from "@lucide/astro";

const values = [
  {
    icon: Users,
    title: "Comunidad > Clientes",
    description: "No eres un ticket. Somos un grupo de entusiastas ayudándonos mutuamente. Si tú creces, nosotros crecemos."
  },
  {
    icon: FileText,
    title: "Cuentas Claras",
    description: "Sin letras pequeñas. Mantenemos precios mínimos para que la tecnología sea accesible, sin sorpresas."
  },
  {
    icon: Zap,
    title: "Calidad Accesible",
    description: "Infraestructura premium (Pterodactyl, NVMe). Bajo costo no es bajo rendimiento."
  }
];
---

<section class="py-24 px-6 relative border-b border-white/5">
  <div class="max-w-6xl mx-auto">
    
    <div class="mb-16">
      <h2 class="text-2xl md:text-4xl font-black text-white mb-6">
        LO QUE NOS <span class="text-gradient">MUEVE</span>
      </h2>
      <p class="text-slate-400 text-lg max-w-2xl">
        Principios inquebrantables que definen nuestra arquitectura.
      </p>
    </div>

    <div class="grid md:grid-cols-3 divide-y md:divide-y-0 md:divide-x divide-white/10">
      
      {values.map((val, index) => (
        <div class={`group relative py-8 md:py-0 ${index === 0 ? 'md:pr-12' : index === 1 ? 'md:px-12' : 'md:pl-12'}`}>
          
          <span class="absolute -top-6 left-0 text-9xl font-black text-white/2 -z-10 select-none transition-colors duration-500 group-hover:text-white/5">
            0{index + 1}
          </span>

          <div class="relative">
            <div class="mb-6 text-brand-cyan group-hover:text-brand-magenta transition-colors duration-300 group-hover:drop-shadow-[0_0_15px_rgba(217,70,239,0.5)]">
              <val.icon size={48} stroke-width={1.5} />
            </div>

            <h3 class="text-2xl font-bold text-white mb-4 group-hover:text-brand-cyan transition-colors duration-300">
              {val.title}
            </h3>
            
            <p class="text-slate-400 leading-relaxed font-light">
              {val.description}
            </p>
          </div>

        </div>
      ))}

    </div>

  </div>
</section>