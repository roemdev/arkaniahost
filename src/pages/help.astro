---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import {
  LifeBuoy, BookOpen, Search, Terminal, Activity,
  MessageCircle, Ticket, Mail, AlertOctagon, ArrowRight
} from "@lucide/astro";

// --- DATOS ---

const diagnosticSteps = [
  {
    icon: BookOpen,
    title: "Documentación",
    desc: "Revisa la wiki. El 80% de las dudas ya están resueltas ahí.",
    color: "text-blue-400",
    bg: "bg-blue-500/10",
    border: "border-blue-500/20"
  },
  {
    icon: Search,
    title: "Comunidad",
    desc: "Busca en Discord. Es probable que alguien ya tuviera tu error.",
    color: "text-purple-400",
    bg: "bg-purple-500/10",
    border: "border-purple-500/20"
  },
  {
    icon: Terminal,
    title: "Consola / Logs",
    desc: "¿Qué dice el error? Lee las últimas líneas de tu consola.",
    color: "text-yellow-400",
    bg: "bg-yellow-500/10",
    border: "border-yellow-500/20"
  },
  {
    icon: Activity,
    title: "Estado del Nodo",
    desc: "Chequea status.arkaniahost.xyz para ver si hay incidencias globales.",
    color: "text-green-400",
    bg: "bg-green-500/10",
    border: "border-green-500/20"
  }
];

const contactChannels = [
  {
    icon: MessageCircle,
    name: "Discord",
    time: "Casi inmediata",
    sub: "(Horario Diurno)",
    rec: true, // Recomendado
    color: "text-indigo-400",
    btnColor: "bg-indigo-600 hover:bg-indigo-500",
    cta: "Unirse al Servidor"
  },
  {
    icon: Ticket,
    name: "Ticket Web",
    time: "10 - 30 min",
    sub: "(Horario Diurno)",
    rec: false,
    color: "text-cyan-400",
    btnColor: "border border-cyan-500/30 hover:bg-cyan-500/10 text-cyan-400",
    cta: "Abrir Ticket"
  },
  {
    icon: Mail,
    name: "Email",
    time: "1 - 2 horas",
    sub: "(Horario Diurno)",
    rec: false,
    color: "text-slate-400",
    btnColor: "border border-slate-600 hover:bg-slate-800 text-slate-300",
    cta: "Enviar Correo"
  }
];
---

<Layout title="Centro de Soporte | ArkaniaHost">
  <Navbar />

  <section class="relative pt-32 pb-16 bg-slate-950 overflow-hidden">
    <div class="absolute inset-0 bg-grid-pattern opacity-20"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-cyan-500/5 rounded-full blur-[100px] pointer-events-none"></div>

    <div class="relative z-10 max-w-7xl mx-auto px-4 text-center">
      <!-- <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-slate-900 border border-slate-800 mb-6">
        <LifeBuoy size={16} class="text-cyan-400 animate-pulse" />
        <span class="text-slate-400 font-mono text-xs uppercase tracking-widest">Support Center v1.0</span>
      </div> -->
      <h1 class="text-4xl md:text-6xl font-black text-white tracking-tighter uppercase mb-6">
        ¿Servidor <span class="text-cyan-500 italic text-shadow-neon-cyan">Fallando?</span>
      </h1>
      <p class="text-slate-400 text-lg md:text-xl max-w-2xl mx-auto font-light">
        No entres en pánico. Sigue el protocolo de diagnóstico antes de contactar a un operador humano.
      </p>
    </div>
  </section>

  <section class="py-16 bg-slate-900/50 border-y border-white/5 relative">
    <div class="max-w-7xl mx-auto px-4">
      <h2 class="text-center text-2xl font-black text-white uppercase tracking-widest mb-12 flex items-center justify-center gap-3">
        <Terminal size={24} class="text-fuchsia-500" /> Protocolo de Auto-Diagnóstico
      </h2>

      <div class="relative">
        <div class="hidden md:block absolute top-1/2 left-0 w-full h-px bg-gradient-to-r from-transparent via-slate-700 to-transparent -translate-y-1/2 z-0"></div>

        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 relative z-10">
          {diagnosticSteps.map((step, index) => (
            <div class="group relative bg-slate-950 border border-slate-800 hover:border-fuchsia-500/50 p-6 rounded-2xl transition-all duration-300 hover:-translate-y-2">
              <div class="absolute -top-3 -right-3 w-8 h-8 bg-slate-900 border border-slate-700 rounded-full flex items-center justify-center font-mono text-xs text-slate-500 group-hover:text-white group-hover:border-fuchsia-500 transition-colors">
                {index + 1}
              </div>
              
              <div class={`w-12 h-12 rounded-lg ${step.bg} ${step.border} flex items-center justify-center mb-4 border`}>
                <step.icon class={step.color} size={24} />
              </div>
              
              <h3 class="text-white font-bold uppercase text-sm tracking-wider mb-2">{step.title}</h3>
              <p class="text-slate-400 text-sm leading-relaxed">{step.desc}</p>

              <div class="md:hidden flex justify-center mt-4 text-slate-700">
                {index < diagnosticSteps.length - 1 && <ArrowRight size={20} />}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="py-24 bg-slate-950">
    <div class="max-w-7xl mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-black text-white uppercase tracking-tighter mb-4">Canales de <span class="text-cyan-500 italic text-shadow-neon-cyan">comunicación</span></h2>
        <p class="text-slate-500 font-mono text-sm">Selecciona tu vía de comunicación</p>
      </div>

      <div class="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto items-start">
        {contactChannels.map((channel) => (
          <div class={`relative p-8 rounded-3xl border transition-all duration-300 ${channel.rec ? 'bg-slate-900/80 border-fuchsia-500/50 shadow-[0_0_40px_rgba(217,70,239,0.15)] scale-105 z-10' : 'bg-slate-950 border-white/5 hover:border-white/10'}`}>
            
            {channel.rec && (
              <div class="absolute -top-4 left-1/2 -translate-x-1/2 bg-fuchsia-600 text-white px-4 py-1 rounded-full text-[10px] font-bold uppercase tracking-widest shadow-lg">
                Recomendado
              </div>
            )}

            <div class="flex flex-col items-center text-center">
              <div class={`p-4 rounded-full bg-slate-900/50 mb-6 ${channel.color}`}>
                <channel.icon size={32} />
              </div>
              
              <h3 class="text-2xl font-black text-white uppercase italic mb-1">{channel.name}</h3>
              <div class="font-mono text-lg font-bold text-slate-200 mb-1">{channel.time}</div>
              <p class="text-slate-500 text-xs uppercase tracking-widest mb-8">{channel.sub}</p>
              
              <button class={`w-full py-3 rounded-lg font-bold uppercase text-xs tracking-widest transition-all ${channel.btnColor}`}>
                {channel.cta}
              </button>
            </div>
          </div>
        ))}
      </div>

      <div class="mt-12 text-center">
        <p class="text-slate-500 text-xs font-mono uppercase tracking-widest opacity-70">
          Nota: Fines de semana y días festivos el tiempo de respuesta podría verse ligeramente incrementado.
        </p>
      </div>
    </div>
  </section>

  <section class="py-12 bg-slate-950 border-t border-white/5">
    <div class="max-w-4xl mx-auto px-4">
      <div class="bg-red-950/20 border border-red-500/30 rounded-2xl p-6 md:p-8 flex flex-col md:flex-row items-center gap-6 relative overflow-hidden group hover:border-red-500/50 transition-colors">
        <div class="absolute top-0 right-0 w-64 h-64 bg-red-600/10 rounded-full blur-[80px] -translate-y-1/2 translate-x-1/2 pointer-events-none animate-pulse"></div>

        <div class="p-4 bg-red-900/20 rounded-xl border border-red-500/20 text-red-500 shrink-0">
          <AlertOctagon size={40} />
        </div>

        <div class="flex-1 text-center md:text-left">
          <h3 class="text-xl font-black text-white uppercase tracking-tight mb-2 flex items-center justify-center md:justify-start gap-2">
            Fallo Crítico detectado <span class="hidden md:inline-block w-2 h-2 bg-red-500 rounded-full animate-ping"></span>
          </h3>
          <p class="text-red-200/70 text-sm leading-relaxed mb-4">
            En caso de <strong class="text-red-400">fallo de seguridad, pérdida de datos o caída total</strong> de la infraestructura.
          </p>
          <div class="text-xs font-mono text-red-400/50 uppercase tracking-widest">
            Protocolo: Marcar ticket como <span class="text-red-400 font-bold decoration-red-500 underline">URGENTE</span>
          </div>
        </div>

        <button class="px-6 py-3 bg-red-600/10 hover:bg-red-600 text-red-500 hover:text-white border border-red-500/50 rounded-lg font-bold uppercase text-xs tracking-widest transition-all shrink-0">
          Reportar Emergencia
        </button>
      </div>
    </div>
  </section>

  <Footer />
</Layout>